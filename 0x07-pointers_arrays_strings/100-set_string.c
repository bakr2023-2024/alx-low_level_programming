#include "main.h"

/**
 * set_string - Entry point
 * @s: string
 * @to: ambola
 * Return: void
 */

void set_string(char **s, char *to)
{
	int i;

	for (i = 0; to[i] != '\0'; i++)
		*(s + i) = *(t + i);
	*(s + i) = *(t + i);
}
