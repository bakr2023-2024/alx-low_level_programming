#include <stdio.h>
#include "main.h"
/**
 * add - adds two numbers and returns the result
 * @a: first number
 * @b: second number
 * Return: result of a+b
 */

int add(int a, int b)
{
	return (a + b);
}
